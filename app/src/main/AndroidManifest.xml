<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.paulapps.kereseresapp">

    <!--Para Google+ APIs-->
    <uses-sdk android:minSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.login_signup.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.login_signup.SignUpActivity" />
        <activity android:name=".activities.ListViewActivity" />
        <activity android:name=".activities.login_signup.MainActivity" />
        <activity
            android:name=".activities.perfil.PerfilActivity"
            android:label="@string/title_activity_perfil"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.login_signup.ResetPasswordActivity" />
        <activity
            android:name=".activities.DetallesPedido"
            android:parentActivityName=".activities.ListViewActivity" />
        <activity android:name=".activities.crear_pedidos.CrearDemandaActivity" />
        <activity android:name=".activities.crear_pedidos.CrearOfertaActivity" />
        <activity android:name=".activities.ver_pedidos.verPedidosListViewActivity" />
        <activity android:name=".activities.ver_pedidos.VerDetallePedidoActivity"></activity>
    </application>

</manifest>