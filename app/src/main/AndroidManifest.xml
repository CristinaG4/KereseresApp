<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.paulapps.kereseresapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logofondo4azulclaro"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logofondo4azulclaro"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.login_signup.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.login_signup.SignUpActivity" />
        <activity android:name=".activities.ListViewActivity" />
        <activity android:name=".activities.login_signup.MainActivity" />
        <activity
            android:name=".activities.perfil.PerfilActivity"
            android:label="@string/title_activity_perfil"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.perfil.EditPerfilActivity"
            android:label="@string/title_activity_edit_perfil"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.login_signup.ResetPasswordActivity" />
        <activity android:name=".activities.DetallesPedido" />
        <activity android:name=".activities.NavigationDrawerActivity"></activity>
    </application>

</manifest>